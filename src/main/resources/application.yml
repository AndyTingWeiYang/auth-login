server:
  port: 8080
  servlet:
    context-path: /auth-login

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth-login
    username: root
    password: password
  jpa:
    hibernate:
      show-sql: true


